@model IEnumerable<Discount>

<div class="row row-cols-1 row-cols-lg-3">
    @foreach (Discount d in Model)
    {
        <div class="col discounts px-2 py-4">
            <h3>@d.Title</h3>
            <div class="mb-4">
                @d.Description
            </div>
            <i>Discount expires: @d.EndTime.ToString("MM/dd/yyyy")</i><br />
            <a href="#" class="code" data-code="@d.Code" data-product="@d.Title"><i class="far fa-hand-point-right"></i> Discount Code</a>
        </div>
    }
</div>

<div class="bg-primary m-1 p-1 text-white"><h4>Discount Listing</h4></div>
<ul>
  @foreach (Discount discount in Model)
  {
    <li>@discount.Title</li>
  }
</ul>
<hr />
<a asp-action="AddDiscount">New Discount</a>
@section Scripts {
    <script type="text/javascript" src="~/js/client.js"></script>
}
